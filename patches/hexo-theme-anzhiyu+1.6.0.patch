diff --git a/node_modules/hexo-theme-anzhiyu/layout/includes/widget/card_recent_post.pug b/node_modules/hexo-theme-anzhiyu/layout/includes/widget/card_recent_post.pug
index 2a29493..47b1b74 100644
--- a/node_modules/hexo-theme-anzhiyu/layout/includes/widget/card_recent_post.pug
+++ b/node_modules/hexo-theme-anzhiyu/layout/includes/widget/card_recent_post.pug
@@ -11,10 +11,14 @@ if theme.aside.card_recent_post.enable
         - let title = article.title || _p('no_title')
         - let no_cover = article.cover === false || !theme.cover.aside_enable ? 'no-cover' : ''
         - let post_cover = article.cover
+        - let thumbStyle = ''
+        if post_cover && theme.cover.aside_enable
+          - let coverUrl = url_for(post_cover)
+          - thumbStyle = `--lula-thumb-bg:url('${coverUrl}');background-image:url('${coverUrl}');background-size:168%;background-position:center;background-repeat:no-repeat;`
         .aside-list-item(class=no_cover)
           if post_cover && theme.cover.aside_enable
-            a.thumbnail(href=url_for(link) title=title)
-              img(src=url_for(post_cover) onerror=`this.onerror=null;this.src='${url_for(theme.error_img.post_page)}'` alt=title)
+            a.thumbnail.has-lula-thumb(href=url_for(link) title=title style=thumbStyle)
+              img(src=url_for(post_cover) loading="eager" decoding="async" onerror=`this.onerror=null;this.src='${url_for(theme.error_img.post_page)}'` alt=title)
           .content
             a.title(href=url_for(link) title=title)= title
             if theme.aside.card_recent_post.sort === 'updated'
diff --git a/node_modules/hexo-theme-anzhiyu/scripts/helpers/random.js b/node_modules/hexo-theme-anzhiyu/scripts/helpers/random.js
index 3fe0cdd..9b5f007 100644
--- a/node_modules/hexo-theme-anzhiyu/scripts/helpers/random.js
+++ b/node_modules/hexo-theme-anzhiyu/scripts/helpers/random.js
@@ -18,7 +18,7 @@ hexo.extend.generator.register("random", function (locals) {
 
   let result = `var posts=${JSON.stringify(
     posts
-  )};function toRandomPost(){pjax.loadUrl('/'+posts[Math.floor(Math.random() * posts.length)]);};`;
+  )};function toRandomPost(){if(!Array.isArray(posts)||!posts.length){window.location.href='/archives/';return;}var target=posts[Math.floor(Math.random()*posts.length)]||'/archives/';target='/' + String(target).replace(/^\\/+/, '');if(window.pjax&&typeof window.pjax.loadUrl==='function'){pjax.loadUrl(target);}else{window.location.href=target;}};`;
 
   if (themeConfig.footer.list.enable && randomNumberFriend > 0) {
     result += `var friend_link_list=${JSON.stringify(link_list)};
